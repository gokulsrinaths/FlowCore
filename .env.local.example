# Copy to .env.local and fill from Supabase: Project Settings → API
#
# Auth → URL configuration: add every app origin under Redirect URLs, e.g.
#   http://localhost:3000/**
#   https://your-deployment.vercel.app/**
# (Signup confirmation links use the host you’re on when you register.)

NEXT_PUBLIC_SUPABASE_URL=https://lnqssvyaxlumfakewjrn.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxucXNzdnlheGx1bWZha2V3anJuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzUxNzYwNTEsImV4cCI6MjA5MDc1MjA1MX0.wn6hQ4em4WZ8K3KqFANBTTkoMlvw7qNcm-UiTwtdvO0

# App URL for invite links (no trailing slash). Used when copying / sharing invites in-app.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cron: `/api/cron/reminders` (Vercel cron or manual). Must match Vercel project env if using hosted cron.
# CRON_SECRET=generate-a-long-random-string

# Service role: server-only; used by cron to call reminder/escalation RPCs. Never expose to the client.
# SUPABASE_SERVICE_ROLE_KEY=
